osx_image: xcode9
language: objective-c
podfile: Demo/Podfile
cache: cocoapods
before_install:
  - pod repo update

script: cd Demo ; set -o pipefail && xcodebuild -workspace 'Demo.xcworkspace' -scheme 'Demo' -configuration 'Debug' -sdk iphonesimulator -destination 'name=iPhone 7' build test  | xcpretty -c --test
